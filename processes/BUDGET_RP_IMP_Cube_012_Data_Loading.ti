#region Prolog

#****Begin: Generated Statements***
#****End: Generated Statements****


####################################
# Process : BUDGET_RP_IMP_Cube_012_Data_Loading
# Description : 012 Data Import from FCT_N to BUDGET_N+1
# Created by : MBO
# Date : 19/04/2019
####################################


# ==================================================================================
# --  VARIABLES
# ==================================================================================

sCubeSource = '012_OPS_Monthly_Input' ;
sCubeTarget = sCubeSource;
sCubeWK = '080_Workflow';

sSourceScenario = 'FCT_N';
sTargetScenario = 'BUDGET_N+1';

vTM1USER = IF( SCAN ( '*' , TM1USER() ) <> 0 , DELET( TM1USER() , 2 , 1 ) , TM1USER());

sViewName = 'Vue_Source_Cube_' | sCubeSource | '_' | vTM1USER | '_' |   GetProcessName() ;
sSubsetName = 'Subset_Source_Cube_' | sCubeSource | '_' | vTM1USER | '_' |   GetProcessName() ;

sViewNameTarget = 'Vue_Target_Cube_' | sCubeTarget | '_' | vTM1USER| '_' |   GetProcessName() ;
sSubsetNameTarget = 'Subset_Target_Cube_' | sCubeTarget | '_' | vTM1USER | '_' |   GetProcessName() ;


# ==================================================================================
# -- CHECKING LOCK/UNLOCK STATUS
# ==================================================================================

vAv = CELLGETS ( sCubeWK , sTargetScenario , 'OPS' ,pOrganization  ,'Availability');
IF( vAv@<>'Unlock');
	PROCESSQUIT;
ENDIF;

# ==================================================================================
# -- SOURCE VIEW CREATION
# ==================================================================================

ViewCreate( sCubeSource , sViewName , 1);

#-- Subset creation on dimension Currency
sDim = 'Currency';
IF( SubsetExists( sDim, sSubsetName ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetName );
ELSE;
	SubsetCreate( sDim , sSubsetName, 1 );
ENDIF;
SubsetElementInsert( sDim , sSubsetName , 'LCY' , 1 );
ViewSubsetAssign( sCubeSource , sViewName , sDim , sSubsetName );


#-- Subset creation on dimension Scenario
sDim = 'Scenario';
IF( SubsetExists( sDim, sSubsetName ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetName );
ELSE;
	SubsetCreate( sDim , sSubsetName, 1 );
ENDIF;
SubsetElementInsert( sDim , sSubsetName , sSourceScenario , 1 );
ViewSubsetAssign( sCubeSource , sViewName , sDim , sSubsetName );

#-- Subset creation on dimension Organization
sDim = 'Organization';
IF( SubsetExists( sDim, sSubsetName ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetName );
ELSE;
	SubsetCreate( sDim , sSubsetName, 1 );
ENDIF;

IF ( ELLEV( sDim , pOrganization ) =0 );
	SubsetElementInsert( sDim , sSubsetName , pOrganization , 1 );
ELSE;
	i = 1;
	iMax = DIMSIZ( sDim );
	WHILE( i <= iMax );
		vElt = DIMNM ( sDim , i) ;
		IF ( ELISANC ( sDim , pOrganization, vElt ) <> 0 );
			SubsetElementInsert( sDim , sSubsetName , vElt , 1 );
		ENDIF;
		i = i + 1;
	END;
ENDIF;
ViewSubsetAssign( sCubeSource , sViewName , sDim , sSubsetName );


#-- Subset creation on dimension Months
sDim = 'Months';
IF( SubsetExists( sDim, sSubsetName ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetName );
ELSE;
	SubsetCreate( sDim , sSubsetName, 1 );
ENDIF;
SubsetElementInsert( sDim , sSubsetName , 'Jan' , 1 );
SubsetElementInsert( sDim , sSubsetName , 'Feb' , 2 );
SubsetElementInsert( sDim , sSubsetName, 'Mar' , 3 );
SubsetElementInsert( sDim , sSubsetName, 'Apr' , 4 );
SubsetElementInsert( sDim , sSubsetName , 'May' , 5 );
SubsetElementInsert( sDim , sSubsetName , 'Jun' , 6 );
SubsetElementInsert( sDim , sSubsetName , 'Jul' , 7 );
SubsetElementInsert( sDim , sSubsetName , 'Aug' , 8 );
SubsetElementInsert( sDim , sSubsetName , 'Sep' , 9 );
SubsetElementInsert( sDim , sSubsetName, 'Oct' , 10 );
SubsetElementInsert( sDim , sSubsetName, 'Nov' , 11 );
SubsetElementInsert( sDim , sSubsetName , 'Dec' , 12 );
ViewSubsetAssign( sCubeSource , sViewName , sDim , sSubsetName );


# ==================================================================================
# -- TARGET VIEW RESET
# ==================================================================================

ViewCreate( sCubeTarget , sViewNameTarget , 1);

#-- Subset creation on dimension Scenario
sDim = 'Scenario';
IF( SubsetExists( sDim, sSubsetNameTarget ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetNameTarget );
ELSE;
	SubsetCreate( sDim , sSubsetNameTarget , 1);
ENDIF;
SubsetElementInsert( sDim , sSubsetNameTarget , sTargetScenario , 1 );
ViewSubsetAssign( sCubeTarget , sViewNameTarget , sDim , sSubsetNameTarget );


#-- Subset creation on dimension Organization
sDim = 'Organization';
IF( SubsetExists( sDim, sSubsetNameTarget ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetNameTarget );
ELSE;
	SubsetCreate( sDim , sSubsetNameTarget , 1);
ENDIF;
IF ( ELLEV( sDim , pOrganization ) =0 );

	SubsetElementInsert( sDim , sSubsetNameTarget , pOrganization , 1 );
ELSE;
	i = 1;
	iMax = DIMSIZ( sDim );
	WHILE( i <= iMax );
		vElt = DIMNM ( sDim , i) ;
		IF ( ELISANC ( sDim , pOrganization, vElt ) <> 0 );
			SubsetElementInsert( sDim , sSubsetNameTarget , vElt , 1 );
		ENDIF;
		i = i + 1;
	END;
ENDIF;
ViewSubsetAssign( sCubeTarget , sViewNameTarget , sDim , sSubsetNameTarget );


#-- Subset creation on dimension Months
sDim = 'Months';
IF( SubsetExists( sDim, sSubsetNameTarget ) = 1 );
	SubsetDeleteAllElements( sDim , sSubsetNameTarget );
ELSE;
	SubsetCreate( sDim , sSubsetNameTarget , 1);
ENDIF;
SubsetElementInsert( sDim , sSubsetNameTarget , 'Jan' , 1 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'Feb' , 2 );
SubsetElementInsert( sDim , sSubsetNameTarget, 'Mar' , 3 );
SubsetElementInsert( sDim , sSubsetNameTarget, 'Apr' , 4 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'May' , 5 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'Jun' , 6 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'Jul' , 7 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'Aug' , 8 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'Sep' , 9 );
SubsetElementInsert( sDim , sSubsetNameTarget, 'Oct' , 10 );
SubsetElementInsert( sDim , sSubsetNameTarget, 'Nov' , 11 );
SubsetElementInsert( sDim , sSubsetNameTarget , 'Dec' , 12 );
ViewSubsetAssign( sCubeTarget , sViewNameTarget , sDim , sSubsetNameTarget );


ViewZeroOut( sCubeTarget , sViewNameTarget );

# ==================================================================================
# -- DATASOURCE PARAMETERS
# ==================================================================================

#-- Update the view parameters
ViewExtractSkipZeroesSet ( sCubeSource, sViewName , 1 );
ViewExtractSkipRuleValuesSet ( sCubeSource, sViewName , 1 );
ViewExtractSkipCalcsSet ( sCubeSource, sViewName , 1 );

#-- Assign the view to the process source
DataSourceNameForServer = sCubeSource;
DataSourceCubeView = sViewName;

#endregion
#region Metadata

#****Begin: Generated Statements***
#****End: Generated Statements****
#endregion
#region Data

#****Begin: Generated Statements***
#****End: Generated Statements****


# ==================================================================================
# -- DATA TRANSFERT 
# ==================================================================================



IF(  SCAN('TEUS' , Indicators_Op )<>0 );
	vValueS1 = CELLGETN( sCubeSource ,Currency ,sSourceScenario,  'YTD_Jul',  Activity , Geo_From , Geo_To , Organization ,Customer_Top, 'TOTAL_TEUS' );
ELSE;
	IF( SCAN('CW',Indicators_Op)<>0);
 		vValueS1 = CELLGETN( sCubeSource ,Currency ,sSourceScenario,  'YTD_Jul',  Activity , Geo_From , Geo_To , Organization ,Customer_Top, 'TOTAL_CW' );
	ELSE;

	vValueS1 = CELLGETN( sCubeSource ,Currency ,sSourceScenario,  'YTD_Jul',  Activity , Geo_From , Geo_To , Organization ,Customer_Top, Indicators_Op  );

	ENDIF;

ENDIF;

# Init S1 if S1 = 0
IF( vValueS1 =0 );
	vValueS2 = CELLGETN( sCubeSource ,Currency ,sSourceScenario,  'S2',   Activity , Geo_From , Geo_To , Organization ,Customer_Top, Indicators_Op  );
	vValue = vValueS2/5;
	vDim2 = 'Months';
	k = 1;
	kMax = SubsetGetSize ( vDim2 , sSubsetNameTarget ) ;
	WHILE( k <= kMax );
  		vMonth = SubsetGetElementName ( vDim2 , sSubsetNameTarget , k );

  		CELLPUTN( vValue , sCubeTarget  , Currency ,sTargetScenario,  vMonth,   Activity , Geo_From , Geo_To , Organization ,Customer_Top, Indicators_Op  );
		k= k + 1;
	END;

	## 011 Annual value Correction ##
	vTotal =vValue * 12;
	IF (CELLISUPDATEABLE('011_OPS_Yearly_Input'  , Currency ,sTargetScenario,  Activity , Geo_From , Geo_To , Organization ,Customer_Top, Indicators_Op)<> 0 );
		CELLPUTN(vTotal , '011_OPS_Yearly_Input'  , Currency ,sTargetScenario,  Activity , Geo_From , Geo_To , Organization ,Customer_Top, Indicators_Op);
	ENDIF;
ELSE;
	CELLPUTN( Value, sCubeTarget  , Currency ,sTargetScenario,  Months,   Activity , Geo_From , Geo_To , Organization ,Customer_Top, Indicators_Op  );
ENDIF;



#endregion
#region Epilog

#****Begin: Generated Statements***
#****End: Generated Statements****

#endregion